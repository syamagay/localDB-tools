# Configs for web viewer

mongoDB:
    host: 127.0.0.1
    port: 27017
    db: yarrdb
    userdb: userdb

    # For user authentication
    #username: viewer
    #password: viewerpass

flask:
    host: 127.0.0.1
    port: 5000

summary:
    serial: AA-11
    stage: encapsulation

# python version
python: 2
